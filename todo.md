# todo — ai-advent-9

Состояние работ. Что проект делает — в README и спеках, здесь только продвижение.

## Сейчас — 17.09

- День 14 завершён через внешнюю Codex FSM (`DONE`, 13/13 acceptance):
  самостоятельный `day14/` сохраняет весь день 13 и добавляет отдельные typed-
  инварианты + audit в SQLite. Конфликт запроса блокируется до model call с ID,
  правилом и причиной; допустимый запрос получает system snapshot; нарушающий
  output заменяется до истории/SQLite и не становится workflow proposal.
  Проверки: invariant 8/8 (12 deny, 6 safe explanations, 9 blocked outputs и
  explicit approval), FSM 10/10, workflow 6/6, web 7/7, machine 19/19,
  JS/compile pass; responsive 3/3 без overflow. Немой offline harness-video
  13,16 с показывает deny, explanation и pause → restart → resume. Real-provider
  eval и deploy не выполнялись. Код и evidence опубликованы коммитом `7378fd9`
  в ветке `RomaniumSSS/day13-task-state-machine`. [Описание](day14/README.md),
  [тесты](day14/results/test-report.md), [видео](day14/results/video-report.md),
  [FSM](experiments/day14-codex-machine/state.json). Следующее: Роман загружает
  `day14/demo/day14-invariants.webm` в Google Drive и отдельно сдаёт ссылки.

- Полуавтономный loop day13 завершён отдельной FSM ассистента (`DONE`): человек
  утверждает planning и финальный validation, а после approve harness сам делает
  `execution → validation` до следующей границы. Durable proposal переживает
  restart и pause/resume, связан с version FSM; in-flight и commit-window pause,
  stale/double apply, empty response и turn limit проверены. Каждый stage явно
  получает objective и нужные артефакты из одного snapshot. Offline matrix:
  workflow 6/6, task-state 10/10, web 6/6, live guards 2/2, deterministic campaign
  68/68. Real-provider `run-04`: 3/3 OpenInference calls без fallback/retry,
  restart без дубля, два автономных turns, финальный `done`, 3982+3007 tokens,
  `$0.00043575`; cumulative `$0.0021920143/$0.005`. Secret scan прошёл. Final
  review — `APPROVED`, blockers/warnings нет. Код и evidence опубликованы
  коммитом `9ee89dc` в ветке `RomaniumSSS/day13-task-state-machine`; deploy не
  выполнялся.
  Онлайн-панель больше не режет ответы на 300 токенах: default и текущий запуск —
  8000, параметр доступен через `--max-tokens`; web/workflow regressions зелёные.
  [Состояние машины](experiments/semi-autonomous-loop-machine/state.json),
  [live report](day13/results/live-workflow/run-04/report.md),
  [review](experiments/semi-autonomous-loop-machine/runs/17-final-review-report.md),
  [видео](https://drive.google.com/file/d/15xgtpOf5CTi4Qeyk7IVt0Tx5zs6SRAKb/view?usp=sharing).
  Роман принял работу. Новый немой live-ролик: 49,68 с, один настоящий call,
  pause/restart и переходы до done; публичное скачивание проверено без авторизации.
  Следующее: сдача ссылки в таблицу — отдельное действие.
- Модульная сборка контекста day13 завершена отдельной FSM ассистента:
  `AgentCapabilities` независимо включает профиль, слои памяти, FSM и недавнюю
  историю; default совместим с прежним режимом, отключённые источники не читаются
  и не удаляются. Scratch-suite, 5/5 fake-client конфигураций и read-only review
  прошли. Следующее: отдельный task-contract полуавтономного agent loop.
- День 13 завершён во внешней quality state machine: итоговый цикл дошёл до
  `DONE` без blockers/violations. FSM и решение этапа принадлежат приложению;
  `paused` и `done` не вызывают модель. Campaign live-05 прошла 68/68 ситуаций:
  60/60 реальных рабочих responses, 4/4 pause guards, 4/4 done guards, 12/12
  restart cases, quality 100%, safety failures 0. Накопительный расход всех
  пяти кампаний `$0.00258518` при лимите `$0.02`; неудачные precursor-runs
  сохранены. [Live report](day13/results/live/day13-live-05/report.md),
  [ledger](day13/results/live/day13-live-05/ledger.json),
  [review](experiments/day13-codex-machine/runs/21-terminal-guard-review-report.md),
  [состояние машины](experiments/day13-codex-machine/state.json). Commit, push,
  deploy и сдача не выполнялись. Локальная панель объединена с возможностями дня
  12: профиль, три слоя памяти, области и чат работают рядом с FSM; объединённый
  API-тест, FSM/live-eval тесты, JS syntax check и responsive overflow-check
  прошли. Следующее: ручной просмотр Романом и отдельное решение о публикации/сдаче.
- День 11 технически готов, опубликован и прошёл внутреннее ревью: самостоятельная реализация в `day11/` прошла офлайн-
  проверки и локальная панель прошли; девять вызовов реальной модели подтвердили
  границы трёх слоёв ([результат](day11/results/live-memory.json)). Подготовлено
  [немое видео](day11/demo/README.md), 40,28 с: код, две проверки влияния
  рабочей памяти на ответ, натуральная ошибка области и четыре области
  памяти; браузерный прогон без ошибок на 1280×800, 768×1024 и 375×812. Блокер
  ревью закрыт: после ошибки поля возвращаются к фактической области, занятые
  POST-запросы получают 409 вместо зависания, память читается один раз на ход,
  отрицательные проверки требуют ответ «не знаю». Код и видео опубликованы в
  ветке `RomaniumSSS/day11-memory-layers`. Правильная строка участника
  `Ro (@jxh_uk)` — `A132`, ячейка дня 11 — `L132`; ссылки туда ещё не добавлены.
  Сдача в таблице ещё не завершена. Следующее: внести ссылки в `L132` и
  дождаться внешней проверки.
- День 12 реализован как накопительный агент дня 11 + автоматически подключаемый
  профиль; Codex остаётся внешним контуром качества, runtime — реальный DeepSeek
  через OpenRouter. Массовый production eval завершил 24/24 новых cases без retries:
  память 8/8, состояние 4/4, adversarial 4/4, профиль 1/8; всего 17 pass и 7 честно
  сохранённых quality_fail, safety_fail нет. Один restart сохранил историю и три
  слоя памяти. Общий расход с тремя прежними попытками — $0.00032573. Публичная
  панель `https://day12.89.167.40.172.sslip.io/` остаётся read-only; backend только
  loopback. [Live report](day12/results/mass-live/day12-mass-01/live-report.md),
  [raw report](day12/results/mass-live/day12-mass-01/report.json),
  [VPS smoke](day12/results/mass-live/day12-mass-01/deploy-report.md),
  [visual](day12/results/mass-live/day12-mass-01/visual-report.md),
  [video](day12/results/mass-live/day12-mass-01/video-report.md). Независимый review
  подтвердил ledger, hashes, profile reuse и отсутствие утечек/дублей. Код запушен
  в `RomaniumSSS/day12-user-profile` (`c199fb0`), replay опубликован на Google Drive.
  Строка участника подтверждена: `Ro (@jxh_uk)` — `A132`, ячейка дня 12 — `M132`.
  Ссылки оставлены комментарием к `M132`; сама ячейка остаётся пустой, потому что
  текущий Google-аккаунт имеет только Comment access. Для обычной записи нужен Editor.
  Дополнительно снята живая демонстрация ручного диалога; итоговая немая версия
  лежит локально в `~/Screenshots/День 12 — профиль ассистента — без звука.mp4`
  (2,2 МБ, 102,77 с). Она ещё не загружена и не подменяет опубликованный replay.

## Сделано
- [x] 12.09 — **день 10: код и живой прогон готовы**, не сдан. `day10/` — три
      стратегии контекста без summary (`strategies.py`), фиксированные ключи
      фактов (`facts.py`), ветки поверх сессий (`branches.py`), панель на 8040
      с переключателем. Схема SQLite v4. Шесть офлайн-проверок зелёные.
      Живой прогон 16 ходов на каждой стратегии плюс ветвление: полная история
      180 000 токенов, Facts 67 251 (−62,6%, память 12/15), Sliding 42 360
      (−76,5%, память 3/15). Потрачено ~$0.009 из $0.50. Главное наблюдение:
      полная история назвала отменённый срок, Facts — актуальный.
      [Числа и механизм](day10/README.md). Видео снято: 57,8 с без звука,
      13 ходов из реального прогона плюс один живой, [описание](day10/demo/README.md).
      Следующее: просмотр Романом, загрузка видео и сдача.
- [x] 12.09 — день 9 смержен в `main` и запушен (`4a67cf8`); ветка day10
      отведена от актуального main.
- [x] 11.09 — **день 8 сдан**, подтверждено Романом. Код и проверки:
      `0831454`, [day08/VERIFICATION.md](day08/VERIFICATION.md); видео — в README.
      Прежние пометки «следующее: сдача» ниже исторические. Следующее: день 9,
      сжатие истории; предварительно разобрано задание из экспорта группы.
- [x] Дни 1-5 сданы, `main` запушен. Последний коммит дня 5 — `c4af966`.
- [x] День 5: переезд на OpenRouter, тройка Kimi/DeepSeek/gpt-oss, замеры в README.
- [x] 08.09 — `CLAUDE.md` заведён: границы, инференс, команды запуска, замеренное.
- [x] 08.09 — **день 6 сдан**: `day06/` (агент как отдельная сущность), 10 офлайн-проверок,
      видео снято и смонтировано, ссылки в таблице. Коммиты `9fbc4ee` и `b22103b`.

- [x] 09.09 — **день 7 сдан**: `day07/` (история в SQLite, разговор переживает перезапуск),
      22 офлайн-проверки, живые прогоны CLI и панели, видео смонтировано
      (`~/Screenshots/day07_clean.mp4`, 91 с). Коммит `e48e2cc`, запушен.
- [x] 08.09 — ревью дня 7: закрыт блокер (сбой записи в базу ронял `ask()` и съедал
      уже оплаченный ответ — теперь `Reply.store_error` и «НЕ СОХРАНЁН» в дебаге),
      подтверждение перед стиранием базы в панели, оговорка в README про «по очереди,
      не одновременно», предупреждение со 100 ходов. Не тронуто: `build_panels` знает
      ключи `left`/`right` жёстко (зелёный пункт ревью).

- [x] 09.09 — **день 8: код и видео готовы**: `day08/` — счёт токенов
      до вызова (`tokens.py`, единственное место с `tiktoken`), разбор запроса на
      роль/историю/вопрос/разметку, `/tokens` и `/growth` в терминале, вес следующего
      запроса в панели, отказ вместо переполнения окна по `--context-limit`.
      31 офлайн-проверка (22 от дня 7 + 9 новых), живые прогоны замерены в README.
      День 7 не тронут: правка живёт только в `day08/`. Новая зависимость: `tiktoken`.
      Исходный код закоммичен: `35336fc`. Видеообзор — `day08/demo/day08-demo.mp4`.

- [x] 09.09 — проверка дня 8 по уточнённым критериям: исправлен учёт пустых
      оплаченных ответов и неизвестного usage, добавлена миграция SQLite v2.
      Реально проверены 12 ходов, переполнение окна 131072 и восстановление;
      39 офлайн-проверок, повтор переполнения 11/11. Доказательства:
      [day08/VERIFICATION.md](day08/VERIFICATION.md).
      Код и результаты проверки — в ветке
      `RomaniumSSS/day08-context-tokens-verification`. Следующее: сдача Романом.

- [x] 09.09 — видео переделано по просьбе Романа: [немая запись кода и панели](day08/demo/README.md),
      1:01 после ускорения ×1,5, четыре настоящих API-вызова, счётчики и локальная защита.
      В панель добавлена накопленная сумма расхода. Проверены кадры, отсутствие
      аудио, восстановление суммы после обновления и три ширины экрана.
      Видео загружено Романом: [Google Drive](https://drive.google.com/file/d/1pOyMp1717Ih83KvAF9KnrmES7CE0iBzH/view?usp=sharing).
      Следующее: сдача Романом.

- [x] 11.09 — **день 9: реализация и проверки завершены**. Два режима,
      оба сценария × 3 пары, по два сжатия, пять тестовых скриптов, видео около 62 с (×1,25).
      Учтённые токены −26,1%, известная цена −7,3%; есть отрицательный прогон
      и потери фактов. [Доказательства](day09/VERIFICATION.md), [видео](day09/demo/README.md).
      Общий известный расход $0.112835, с резервом неизвестного менее $0.159
      при бюджете $0.50. Следующее: просмотр и сдача Романом; день 9 не сдан,
      код и ускоренное видео запушены: `2eab75f`, ветка
      `RomaniumSSS/day09-history-compression`. Технических блокеров нет.

## В работе
- [ ] Панель молчит во время долгого прогона — `AICODE-ASK` в `day05/web.py:223`.
      Решено делать вариант A: колбэк прогресса + фоновый поток + `GET /api/progress`.
      Правит `compare.py`, `web.py`, фронт. **Отдельной веткой**, день 5 уже сдан.
      Ограничение: колбэк вызывать между вызовами моделей, внутрь `ask()` не лезть,
      иначе поедет `elapsed`.

- [x] 08.09 — **три «ничего» разведены** (хвост дня 6, закрыт в дне 7): пустой ответ
      не засчитывается ходом, не пишется ни в память, ни в базу, а причина названа —
      обрыв по лимиту или добровольное молчание. `Reply.empty` + отдельные тексты
      в терминале и панели. Проверено живым прогоном на `--max-tokens 60`.
      День 6 не трогали: он сдан, правка живёт только в `day07/`.

## Дальше

Решения по окружению от 08.09 (хуки, правила, что и почему выключено) —
в `~/Projects/agent-core-dojo/HANDOFF.md`. Сессия шла оттуда, здесь только следы.

## Отвергнуто
- Дешёвый вариант для панели (только счётчик секунд на фронте): при зависании тикал бы
  так же бодро, то есть врал бы именно в том случае, ради которого ставится.
- Индексация проекта эмбеддингами: репозиторий маленький, `grep` быстрее и точнее.
