# HANDOFF

Пишется перед стиранием контекста. Здесь только то, чего нет в репозитории:
рассуждения, отвергнутое, замеренное. Прогресс по задачам — в `todo.md`,
что проект делает — в `README.md` и `CLAUDE.md`.

Снимок: 17.09.2026, день 14 реализован локально через внешнюю Codex FSM и дошёл
до `DONE`; commit, push, deploy и сдача не выполнялись. Детальный прогресс — в
`todo.md`, устройство и команды — в `day14/README.md`.

## Где остановились

Текущая ветка `RomaniumSSS/day13-task-state-machine`; локальный и удалённый HEAD
совпадают на `bd38087`. Основная реализация — `9ee89dc`. Код:
`https://github.com/RomaniumSSS/ai-advent-9/tree/RomaniumSSS/day13-task-state-machine`.
Видео: `https://drive.google.com/file/d/15xgtpOf5CTi4Qeyk7IVt0Tx5zs6SRAKb/view?usp=sharing`;
публичное скачивание без авторизации проверено. Ссылка в таблицу ещё не внесена:
это отдельное внешнее действие.

День 13 принят Романом. Финальная проверка: FSM 10/10, workflow 6/6, web 6/6,
детерминированная кампания 68/68, Codex-machine 19/19, compileall и JS syntax —
pass. Реальный полный loop `run-04`: 3/3 вызова, финальный `done`, отчёт в
`day13/results/live-workflow/run-04/report.md`. Ролик 49,68 с содержит один
настоящий planning-вызов, pause/restart и кодовые переходы до `done`; подробности
в `day13/results/video-report.md`.

Локальная online-панель на `http://127.0.0.1:8043` отвечала непосредственно перед
handoff и использовала временную БД `/tmp/day13-online.zhqjBU/day13.db`. После
`/clear` процесс обязательно проверить заново; handoff не гарантирует его жизнь.

День 14 находится в `day14/`: отдельные typed-инварианты и audit в SQLite,
preflight до model call, system snapshot и response guard до persistence. Внешняя
машина — `experiments/day14-codex-machine/`, состояние `DONE`, 13/13 acceptance.
После adversarial review policy различает 12 конфликтов, 6 безопасных обсуждений,
9 нарушающих outputs и positive approval; отрицание «подтверждение не нужно» не
обходит business rule. Офлайн-видео 13,16 с лежит в
`day14/demo/day14-invariants.webm`; это harness-demo, не real-provider eval. Точка
продолжения: ручной просмотр Романом, затем отдельное решение о commit/push,
загрузке видео и сдаче.

## Решения дня 11, которые не следует переигрывать без причины

- Это сфокусированная сдача задания, а не готовый продукт. В Telegram автор
  разрешил и отдельный недельный проект, и накопительный проект (`message2854`).
- Три слоя разделены по областям `session`, `task`, `user`; полная история
  отдельно хранится в `messages`.
- Явные кнопки сохранения соответствуют условию: ручная и автоматическая
  раскладка разрешены (`message2793`, `message2800`).
- RAG не требуется (`message2798–2799`). Интеграцию прежних дней разумно делать
  после сдачи дня 11, а не расширять её перед отправкой.
- Натуральная ошибка области на видео — намеренная проверка границы: повторное
  использование session id под другой задачей отклоняется, а UI возвращается к
  фактической области.

## Что доказано в дне 11

- Офлайн-проверки и браузерная регрессия прошли; финальное ревью — `APPROVED`.
- Живой прогон: 9 вызовов, 9 строгих проверок; 2677 входных и 2085 выходных
  токенов, `$0.000391224`. Артефакт: `day11/results/live-memory.json`.
- Видео: `day11/demo/day11-memory-fast.mp4`, 40,28 с, 1440×1100, H.264,
  без звука. При записи API не вызывался: показан сохранённый ответ реального
  прогона и временная БД.
- Исправлены найденные ревью проблемы: восстановление UI после ошибки области,
  409 для конфликтующих мутаций, одно чтение памяти на ход и строгие отрицательные
  проверки с ответом «не знаю».

Учебный смысл видео автора и цель недели записаны в
`/Users/ramanpazharytski/Projects/agent-core-dojo/challenge/agent-patterns.md`;
точка учебного разбора — в handoff того проекта.

## Замеренное 12.09.2026 (стоило денег, не переснимать зря)

Сценарий «Планирование проекта», 16 ходов, DeepSeek V4 Flash, OpenInference FP8:

| Стратегия | Вызовов | Вход | Выход | Всего | Цена | Память |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| Полная история | 16 | 159 903 | 20 097 | 180 000 | $0.003591 | 12/15 |
| Sticky Facts | 32 | 54 494 | 12 757 | 67 251 | $0.003195 | 12/15 |
| Sliding Window | 16 | 31 675 | 10 685 | 42 360 | $0.002122 | 3/15 |

Вход первого хода одинаков у всех — 91 токен; к шестнадцатому 20 358 / 2 286 /
2 185. Служебные вызовы Facts: 19 668 токенов, $0.000833 — пятая часть её счёта.
Потрачено за сессию около $0.012 из $0.50; в ledger висит ~$0.0023 удержания
за вызовы без usage.

## Главное открытие, и оно неочевидное

**Полная история ответила неправильно там, где обрезанные ответили верно.**
На всех трёх контрольных ходах она назвала «актуальный срок: 18 октября» —
дату, отменённую на шестом ходу. Sliding и Facts назвали 25 октября.

Полный контекст гарантирует наличие данных, а не правильный выбор из них: обе
даты лежали рядом, и модель взяла не ту. Facts выигрывает не объёмом памяти,
а тем, что отмена там — отдельный ключ `отменённое_решение`, а не ещё одна
реплика в куче.

Это записано в `day10/README.md` и в корневом `README.md`. Один прогон каждой
стратегии — наблюдение, не статистика; повторять на трёх прогонах никто не
просил, и денег на это не закладывалось.

## Отвергнутое в дне 10 (не переигрывать без новой причины)

- **Свободные ключи facts.** Извлекатель плодит синонимы: «цель» → «задача» →
  «что делаем». Три записи об одном, старая не перезаписывается, в запрос
  уезжают все три. Взято из сдачи `arnyigor/ai-advent-challenge`, где фиксацию
  ввели по этой же причине. Плата названа и принята: факт вне списка не
  запомнится — на видео это видно, исключённая онлайн-оплата в память не попала.
- **Удаление факта пустым значением или `null`.** Так сделано у
  `ArtemBotnev/ai_agent`; у `arnyigor` это пробовали и откатили — модель
  «прибирает» то, что сочла неактуальным, и пропажа молчаливая. Оставлены только
  дополнение и перезапись.
- **Facts эвристикой вместо вызова модели.** У `ilushkius` так получилось
  26 записей вместо 12: эвристика тащит «эхо» из ответов ассистента.
- **Суммаризация четвёртой стратегией.** Задание дня 10 явно «без summary»,
  день 9 её уже показал. Мёртвый код читался бы как часть ответа.
- **Ветка как отдельная таблица сообщений.** Сессия в `store.py` уже даёт
  изоляцию, переживающую перезапуск. Вторая система для того же разъехалась бы
  с первой, и заметили бы это, когда в одной ветке всплыла бы чужая реплика.
- **Ветка как ссылка на общий префикс вместо копии.** Экономнее, но тогда всякий
  читатель истории обязан знать про склейку, а `clear()` одной ветки уносил бы
  начало другой.
- **Две панели рядом, как в дне 9.** Там сравнивались два режима на одном
  вопросе. Здесь сравниваются три стратегии на одном разговоре — это показывает
  переключатель, а не вторая колонка.

## Ловушки — выглядит поломкой, но это не она

- **429 от OpenInference на середине прогона.** Провайдер закреплён и fallback
  выключен намеренно (иначе поедет столбец расхода), поэтому лечение — только
  пауза. В `run_strategies.py` три повтора по 20 секунд, повторы пишутся в
  результат отдельным списком. Первый живой прогон упал на 3-м ходу до того, как
  это появилось. Прогон трёх стратегий занимает около 35 минут — это нормально.
- **`serve_recording.py` нельзя останавливать через `kill`/`pkill`.** По SIGTERM
  не отрабатывает `finally`, и `panel-results.json` с отчётом о бюджете не
  пишутся. Только Ctrl+C. В этой сессии отчёт пришлось восстанавливать из ledger.
- **Репетиция записи существует и стоит ноль.** `serve_recording.py --offline`
  плюс `record.py` прогоняют всю хореографию на подставной модели. Проверять
  порядок кликов на платных вызовах — дорогой способ узнать, что кнопка
  называется иначе.
- **`cd` в Bash-инструменте меняет рабочий каталог на все следующие вызовы.**
  В этой сессии после `cd day10/demo` относительные пути перестали находиться,
  и это выглядело как пропавший каталог.
- **Диагностика Pyright на `day10/*.py`** («Import "strategies" could not be
  resolved», «Cannot access attribute ... for class "object"») — унаследована от
  дней 7–9 один в один: `sys.path.insert` вместо пакета и `store: object | None`
  как точка подмены в тестах. Не чинить попутно.
- **Форматтер на PostToolUse переписывает файл после каждой правки.** Перед
  точечным `Edit` по уже изменённому месту читать файл заново, иначе `old_string`
  не совпадёт.

## Что осталось незакрытым с прошлых дней

- Панель дня 5 молчит во время долгого прогона — `AICODE-ASK` в `day05/web.py:223`,
  решение (вариант A: колбэк прогресса + фоновый поток + `GET /api/progress`)
  принято, работа не начата. Отдельной веткой, день 5 сдан.
- `build_panels` в панели дня 9 знает ключи `left`/`right` жёстко — зелёный пункт
  ревью дня 7. В дне 10 панель одна, и вопрос там не возникает.
- `/Users/ramanpazharytski/Projects/ai-advent-9/.env` имеет права 644, а CLAUDE.md
  требует 600. Замечено, не тронуто.
